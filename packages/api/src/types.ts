/**
 * Types-only export for tRPC clients (mobile, web).
 * Import with: import type { AppRouter } from "@ummati/api/types"
 * Do NOT import runtime code from @ummati/api in client bundles.
 */
export type { AppRouter } from "./root";
